# s182 — the ruled verdict per machine (the oracle for s180 / is55), and what
# each machine measured at trunk on 2026-09-24: wolf-lang a565d4b9 (debug
# build, `wolf conform-run main.lu --checked|--native --json`), lupin 0.1.38
# (828b5c55…, `lupin conform-run main.lu --json`), kasumi linux x86-64,
# run from this directory (evidence: ~/lanes/s182/evidence/trunk-a565d4b9-witnesses.log).
ruling = "wolf-lang#438 (2026-09-24): align — a read parameter stored plainly is copied, not lent (push's put line)"
clause = "mem.region.edge.elem, mem.tier0.mode.read"
lands_in = "corpus/memory/index_store_read_param.lu"

[expected]
checked = { verdict = "exit(0)", stdout = "m0=1 xs=2\n" }
native  = { verdict = "exit(0)", stdout = "m0=1 xs=2\n" }
lupin   = { verdict = "exit(0)", stdout = "m0=1 xs=2\n" }

[trunk]
checked = { verdict = "fail(E1002)", phase = "mem" }
native  = { verdict = "fail(E1002)", phase = "mem" }
lupin   = { verdict = "exit(0)", stdout = "m0=1 xs=2\n" }
