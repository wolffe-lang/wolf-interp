//! check: fail(E1001)
//! phase: typecheck
//! conforms: mem.region.edge.elem, mem.tier0.move.2, gram.expr.assign
//!
//! s182 (wolf-lang#438, ruled "align" 2026-09-24; RULED, NOT YET BUILT —
//! parked for s180/is55) — `outs[0] = take xs` MOVES, the one store
//! position `[gram.expr.assign]` admits a mode in; the later push to
//! `xs` is E1001 with `copy xs` as the fix-it, and lupin traps
//! `use-after-move`. Today the line does not parse: `fail(E0201)` on
//! all three machines. Moves to `corpus/memory/index_store_take_list.lu`
//! with the implementation.

fn main() -> !int {
    var xs = List[int]()
    (mut xs).push(1)
    var outs = List[List[int]]()
    (mut outs).push(List[int]())
    outs[0] = take xs
    (mut xs).push(4)
    print("outs0={outs[0].len} xs={xs.len}")
    0
}
